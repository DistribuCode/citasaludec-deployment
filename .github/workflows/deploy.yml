name: Deploy Docker Compose

on:
  push:
    branches:
      - qa

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🧹 Clean EC2 deploy directory
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            rm -rf ~/deploy  # 🧨 Elimina toda la carpeta para evitar conflicto archivo/carpeta
            mkdir -p ~/deploy

      - name: 📂 Copy docker-compose.yml to EC2 (as file)
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          source: "deployment/docker-compose.yml"
          target: "~/deploy/docker-compose.yml"  # ✅ Copia explícitamente como archivo

      - name: 🧐 Check docker-compose.yml on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            echo "✅ Listado de archivos en ~/deploy:"
            ls -l ~/deploy
            echo "✅ Contenido de docker-compose.yml:"
            cat ~/deploy/docker-compose.yml || echo "❌ No se encuentra el archivo"

      - name: 🚀 Run Docker Compose
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.6/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose version
            cd ~/deploy
            ls -l
            cat docker-compose.yml
            docker-compose pull
            docker-compose up -d
